syntax = "proto3";
package greetings;

service Greeter {
    // send a greeting
    rpc SayHello (HelloRequest) returns (HelloReply) {}
}

// request message containing the username
message HelloRequest {
    string name = 1;
}

// response message containing the greeting
message HelloReply {
    string message = 1;
}